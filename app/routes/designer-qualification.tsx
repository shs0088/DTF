import { Link } from "react-router";
import { ArrowLeft, Check, FileUp, LockKeyhole } from "lucide-react";
export default function DesignerQualification() { return <main className="auth-shell qualification-shell" dir="ltr"><section className="qualification-card"><Link className="auth-brand" to="/"><span className="brand-mark">◈</span><span>DTF <b>STUDIO</b></span></Link><span className="eyebrow">DESIGNER QUALIFICATION / 3 DESIGNS</span><h1>Show us<br /><em>your point of view.</em></h1><p className="auth-intro">Exactly three qualification designs are required. Each design will be previewed and analyzed before Submit for Review becomes available.</p><div className="qualification-grid">{[1, 2, 3].map((slot) => <div className="qualification-slot" key={slot}><span className="slot-number">0{slot}</span><FileUp size={22} /><b>Upload design</b><small>PNG, JPG, WebP, SVG or PDF</small></div>)}</div><div className="qualification-foot"><span><LockKeyhole size={14} /> Designer Dashboard unlocks after Admin approval.</span><button className="button button-primary" disabled>Submit for review <Check size={16} /></button></div><Link className="auth-back" to="/"><ArrowLeft size={14} /> Back to storefront</Link></section></main>; }
